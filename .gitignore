# ── Dependencias ──
node_modules/

# ── Variables de entorno (NUNCA commitear secretos: SERVICE_ROLE_KEY, etc.) ──
.env
.env.*
!.env.example

# ── Build / compilado ──
dist/
build/
*.tsbuildinfo

# ── Logs ──
logs/
*.log
npm-debug.log*

# ── Sistema / temporales ──
.DS_Store
*.swp
*~

# ── Copia local del repo shadcn-ui/ui (referencia, NO parte de la app) ──
# Son ~5.700 archivos / 55 MB de un monorepo ajeno. Se consulta para copiar
# componentes y styles, pero no se versiona con el proyecto.
ui-main/

# ── Scratch temporal de herramientas de edición (Claude Code / Rhea) ──
.rhea-tmp/
